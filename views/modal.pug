mixin modal
 div.modal-fade(id='modal', tabindex='-1', role='dialog', aria-labelledby='modalTitle', aria-hidden='true')
  div.modal-dialog.modal-lg(role='document')
   .modal-content
    .modal-header
     h2.modal-title.display-4(id='modalTitle') Let's Collaborate!
      button.close(data-dismiss='modal', aria-label='Close')
       span(aria-hidden='true') &times;
    .container-fluid.modal-body
     form(id='contact-form', action='/contact', method='post', role='form')
      fieldset
       .form-group
        label(for='name') Name
        input.form-control.form-control-sm(type='text', name='name', placeholder='Billy', style='width: 30%;')
        label(for='contactInfo') Best way to reach you
        input.form-control.form-control-sm(type='text', name='contactInfo', placeholder='Email or Phone', style='width: 50%;')
        label(for='msg') Message
        textarea.form-control(name='msg', cols="30", rows="10", placeholder="I've got an idea for a project...")
    .modal-footer
     button.btn.btn-primary(type='submit') Send!
    